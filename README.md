# Lab 7
Matthew Gunawan

1. I would setup automated tests in a GitHub Action, that would be triggered when code was pushed. This is the best one, as it will automatically check the code before it is merged or submitted, thus allowing bugs to be caught at an earlier stage rather than at the end of development.

2. No. An end-to-end test is primarily used to test the entire process of a user on the webpage. I would use a unit test to test whether one function is returning an appropriate answer.

3. Navigation mode evaluates the page immediately after loading, which is helpful to check the overall performance of a page. Snapshot mode processes the page as it is now, so it is useful if you want to check such features as accessibility once the page has loaded.

4. There are three areas that we could improve on:
- Improve labels or names of buttons, interactive elements.
- Optimize images for better performance and quick loading of the page.
- Optimize performance by minimizing unused JavaScript or other unused resources.